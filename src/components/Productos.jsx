import React from 'react'

export const Productos = ({listado}) => {
  console.log(listado);
  return (
    /*<ul>
      {listado.map((producto) => (<Card title={producto.title} description={producto.description} image={producto.image} />))}
    </ul>*/<></>
  )
}
